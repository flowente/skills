# {{BRAND_NAME}} — System Prompt

Self-contained. Everything needed to produce on-brand work for {{BRAND_NAME}} without reading anything else.

## What this brand is

{{POSITIONING}}

Tagline: **{{TAGLINE}}**. Style: **{{STYLE_NAME}}**.

Central metaphor: **{{METAPHOR}}**. Copy, motion and illustration all express it. A choice that cannot be justified by the metaphor is off-brand however well executed.

## Package map

`brand.json` is the source of truth; `tokens/*.css` and `tailwind.brand.js` are generated from it. `guidelines.html` is the rendered specimen. `assets/` holds the validated logo sizes. Use tokens, never hard-coded hex.

## Colour

Semantic roles, light and dark: `--bg --surface --surface-2 --border --text --text-2 --muted --accent`.

**Discipline:** {{COLOR_DISCIPLINE}}

Contrast is verified against WCAG AA at build time. If you introduce a new pair, verify it — do not assume it passes because it looks like it should.

## Type

- Display: {{TYPE_DISPLAY}}
- Body: {{TYPE_BODY}}
- Mono: {{TYPE_MONO}}
- Hero size: {{TYPE_HERO}}

## Layout

Content max-width {{LAYOUT_MAXWIDTH}}, side padding {{LAYOUT_SIDEPADDING}}, section rhythm {{LAYOUT_SECTIONPADDING}}, nav height {{LAYOUT_NAVHEIGHT}}.

Grid: {{GRID_COLUMNS}} columns, gutter {{GRID_GUTTER}}, margin {{GRID_MARGIN}}, collapsing at the documented breakpoints. Use it for any new layout — slides, mockups, screens — instead of re-deriving measurements per page.

Radii: {{RADII}}. Shadow policy: {{SHADOW_POLICY}}.

## Imagery

- Photography: {{PHOTOGRAPHY_RULE}}
- Texture: {{TEXTURE_RULE}}
- Iconography: {{ICONOGRAPHY_RULE}}
- Graphic device: {{MARK_SYSTEM}}

## Motion

{{MOTION_RULES}}

Everything respects `prefers-reduced-motion`.

## Voice

Register: {{VOICE_REGISTER}}. Casing: {{VOICE_CASING}}. Punctuation: {{VOICE_PUNCTUATION}}.

{{VOICE_RULES}}

Words the brand uses: {{WORDS_USED}}. Words it never uses: {{WORDS_BANNED}}.
CTAs: {{CTA_EXAMPLES}}.

## Never do

{{NEVER_DO_LIST}}

## Not documented — ask, don't invent

{{UNDOCUMENTED_LIST}}
