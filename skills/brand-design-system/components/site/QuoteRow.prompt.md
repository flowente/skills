Testimonial row: 4 hairline-divided columns, each with a display-font client type (not a real name), a short quote in curly quotes, and a mono role line. Never photos or logos.

```jsx
<QuoteRow />
<QuoteRow quotes={[{ name: "PMI manifattura", text: "…", who: "Direttore operations" }]} />
```
