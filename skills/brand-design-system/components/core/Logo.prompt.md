Brand lockup: the "unravelling knot" mark in Flow Blue + lowercase "flowente" wordmark in Space Grotesk ink. Also exports `LogoMark` (mark alone).

```jsx
<Logo style={{ fontSize: "1.25rem" }} />
<LogoMark style={{ height: 32 }} />
```

Size via font-size on Logo. Color only on the mark, never the wordmark.
