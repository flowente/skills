Solid Flow Blue shape (square/circle/triangle/blob) with hand-drawn wobbly edges — sits BEHIND marks, never carries text.

```jsx
<AccentShape kind="blob" style={{ width: 120, height: 120 }} />
```

Fill comes from `--accent` via `.shape-fill`. Requires `<SvgFilters />` on the page.
