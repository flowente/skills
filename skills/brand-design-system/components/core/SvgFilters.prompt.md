Defines the three hand-drawn "pencil" SVG filters (boiling frames) — render once per page before any FlowMark/AccentShape.

```jsx
<SvgFilters />
```

No props. Without it, marks and shapes render un-displaced (clean vector, no hand-drawn wobble).
