# Website UI kit — Flowente homepage

Faithful recreation of `app/page.tsx` from the source repo, composed entirely from the design-system components (Nav, Hero, FeatureSection ×2, CardGrid, QuoteRow, Footer) plus a floating light/dark theme toggle.

- `index.html` — the full page. Interactive: theme switch, boiling marks, hover states.
- Built from: `app/page.tsx`, `app/layout.tsx`, `app/template.tsx`, `components/*.tsx` at commit `d5647493`.
