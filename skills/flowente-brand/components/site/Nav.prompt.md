Sticky site header — translucent blurred background over the page, hairline bottom border.

```jsx
<Nav links={["Studio", "Advisory", "Lavori", "Blog"]} cta="Parliamone" />
```

66px tall, max-width 1160px inner. Login is always ghost, CTA always primary ink.
