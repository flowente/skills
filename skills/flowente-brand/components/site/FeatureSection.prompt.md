Alternating two-column feature band: display headline with a line break, ≤2-sentence text, optional ghost CTA, and a rounded surface-2 panel holding a MarkBadge.

```jsx
<FeatureSection title={<>Meno lavoro ripetitivo.<br />Più cose che scorrono.</>} text="Un agente legge, cerca, decide e agisce…" ctaLabel="Scopri lo Studio" mark="onde" shape="square" />
<FeatureSection reverse mark="flusso" shape="blob" title="…" text="…" />
```

Alternate `reverse` down the page; vary mark + shape per section.
