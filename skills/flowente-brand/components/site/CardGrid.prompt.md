"Storie e risorse" section: centered display heading + one-line sub, then a 3-column grid of Cards (each with a distinct mark + shape).

```jsx
<CardGrid />
<CardGrid heading="Storie e risorse" cards={[{ title, meta, tag, mark: "flusso", shape: "square" }, …]} />
```
